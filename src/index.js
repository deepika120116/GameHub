const isSubsequent = (arr, subsequent) => {
    let prevIndex = 0;
    for (let i = 0; i < subsequent.length; i++){
        
    }
}
console.log(isSubsequent([5, 1, 22, 25, 6, -1, 8, 10], [1, 6, -1, 10,]));
